package com.oops.generics;
public class WildCards {
    //here the t should be either Number or the subclasses of Number
    // public class CustomArrayList <T extends Number>
    // the <? extends Number> is also a wild card that allows only the classes that are included in the Number class
    //
    /*
    public void getList (List<Number> list)
    here we can only be able to pass the number type. by the way to pass the number type and subclasses of number then
     syntax is = List <? extends Number> list
     */
}
